{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mobile",
  "description": "Capability for the main window (mobile). The desktop-only plugins — autostart, updater, process, window-state — are not compiled on mobile, so their permissions are omitted here.",
  "platforms": ["iOS", "android"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-background-color",
    "opener:default",
    "opener:allow-open-url",
    "notification:default",
    "os:default",
    "deep-link:default"
  ]
}
